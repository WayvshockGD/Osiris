import { Command } from "../../core/command/Command";
 
export = new Command({
    name: "modlog",
    description: "Sets the modLog.",
    enabled: true,
    category: "Settings",

    execute: ({  }) => {}
})